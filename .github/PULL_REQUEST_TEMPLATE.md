<!--
Base branch check: this PR must target `dev`, not `main`. `main` is the
protected, stable branch and only moves via merges from `dev` — see
CONTRIBUTING.md#branching-and-pull-requests. If the base shown above isn't
`dev`, change it before requesting review.
-->

## What

## Why

## Checklist

- [ ] This PR targets `dev`, not `main`
- [ ] `cargo fmt --all -- --check`
- [ ] `cargo clippy --workspace --all-targets -- -D warnings`
- [ ] `cargo test --workspace`
